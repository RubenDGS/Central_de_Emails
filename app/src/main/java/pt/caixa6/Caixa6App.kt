

            manager.createNotificationChannel(
                NotificationChannel(
                    "keepalive",
                    "Central de Emails",
                    NotificationManager
                        .IMPORTANCE_LOW
                )
            )
        }
    }
}
